from pathlib import Path

ROOT_DIR = Path(__file__).resolve(strict=True).parent
SOURCE_DIR = ROOT_DIR / 'src'
TRAIN_DIR = ROOT_DIR / 'src' / 'train'
MODELS_DIR = ROOT_DIR / 'models'
DATASET_DIR = ROOT_DIR / 'datasets'
